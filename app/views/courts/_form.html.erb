<%= form_for(@court) do |f| %>
  <% if @court.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@court.errors.count, "error") %> prohibited this court from being saved:</h2>

      <ul>
      <% @court.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :city %><br>
    <%= f.text_field :city %>
  </div>
  <div class="field">
    <%= f.label :court_number %><br>
    <%= f.number_field :court_number %>
  </div>
  <div class="field">
    <%= f.label :surface %><br>
    <%= f.text_field :surface %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
